export enum Download {
    epub = "epub",
}
  
export enum Filter {
    ebooks = "ebooks",
    "free-ebooks" = "free-ebooks",
    full = "full",
    "paid-ebooks" = "paid-ebooks",
    partial = "partial"
}

export enum LibraryRestrict {
    newest = "newest",
    relevance = "relevance"
}

export enum OrderBy {
    "my-library" = "my-library",
    "no-restrict" = "no-restrict"
}

export enum PrintType {
    all = "all",
    books = "books"
}

export enum Projection {
    full = "full",
    lite = "lite"
}